---
slug: mdx-blog-post
title: MDX 
authors: [pm1017]
tags: [mdx]
---

用 [MDX](https://mdxjs.com/) 写博客文章，可以让你在Markdown中使用React组件。

{/* truncate */}

例如，使用 JSX 创建一个交互式按钮：

```js
<button onClick={() => alert('button clicked!')}>Click me!</button>
```

<button onClick={() => alert('button clicked!')}>Click me!</button>
